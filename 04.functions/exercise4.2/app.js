    /**
     * rand10 returns a random integer between 1 and 10. 
     * This function takes no parameters.
     *  */ 

    let rand10 = () => {
        return Math.ceil(Math.random()*10);
    }

    console.log(rand10())